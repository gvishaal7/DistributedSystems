Compile the code using make file

1) Run TCPserver in on VMs 
NOTE: the server has been hardcoded to listen on port 8080
2) Enter the other 4 server IPs manually
Note - Comment the lines from 68  to  79 and remove the comment for lines 80 to 84 to use static server IPs
3)  Run the TCPclient program
4) Enter the server IP to connect the client to


Log files create - tcpAnalysisReport (client), tcpClientTime (client), tcpServerLog (server), keyValuePair (server)